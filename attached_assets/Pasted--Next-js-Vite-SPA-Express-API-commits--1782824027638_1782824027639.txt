مرحباً،

الترحيل من Next.js إلى Vite SPA + Express API الذي تم في الـ commits الأخيرة
كان خطأ معمارياً. أضاع كل ضمانات السلامة المالية التي بُنيت سابقاً:
- لا transactions على تعديلات متعددة الصفوف
- لا idempotency على convert-to-sale (بيع مزدوج ممكن)
- لا optimistic concurrency (تعديلات متزامنة تكتب فوق بعضها)
- لا CHECK constraints على قاعدة البيانات
- لا migrations نشطة (الكود لا يطابق قاعدة البيانات الحية)
- المصادقة رمز في localStorage بدلاً من httpOnly cookie

المطلوب:

1. ارجع إلى commit 157a9e9 (آخر commit سليم مع Next.js + كل الإصلاحات).
   git reset --hard 157a9e9
   git push --force

2. أعد بناء الميزات الجديدة التي أُضيفت بعد 157a9e9 فوق بنية Next.js،
   واحدة تلو الأخرى، مع الحفاظ على نفس جودة الكود والضمانات:
   
   أ) كتالوج المكونات (catalog):
      - جدول component جديد في lib/db (uuid id، name، default_cost_cents، unit، notes، deleted_at، created_at، updated_at)
      - CHECK constraints + index على name
      - migration جديد 0003
      - feature module: src/features/catalog/ (actions.ts، queries.ts، schema.ts، db.ts، hooks.ts، components/)
      - Server Actions مع transactions + optimistic concurrency + idempotency
      - route /catalog
      - CatalogPicker component يُفتح من ComponentsEditor (bottom sheet)
      - عند الاختيار: ينسخ name + default_cost_cents إلى order_component (لقطة، لا FK)
   
   ب) مكتبة القوالب (snippets):
      - جدول snippet جديد (id، title، body، category، deleted_at، created_at، updated_at)
      - CHECK constraints + index على category
      - migration جديد 0004
      - feature module: src/features/snippets/
      - Server Actions مع transactions
      - route /snippets
      - UI: قائمة بطاقات + bottom sheet للعرض مع زر COPY كبير
      - نسخ عبر navigator.clipboard.writeText
   
   ج) عرض تقويمي للطلبات (calendar):
      - component جديد OrdersCalendar في features/orders/components/
      - يستخدم useInfiniteOrders أو استعلام جديد getOrdersForCalendar
      - lazy-loaded (dynamic import) لتفادي زيادة الـ bundle
   
   د) أزرار سريعة لحالة الطلب:
      - updateOrderStatus Server Action في features/orders/actions.ts
      - optimistic concurrency (WHERE id = $1 AND updated_at = $2)
      - row-level locking (SELECT FOR UPDATE)
      - في OrderDetail.tsx: stepper أفقي للحالات (draft → sent → confirmed → delivered)
   
   هـ) تكاليف علوية (overhead costs):
      - أضف عمود overhead_cost_cents integer not null default 0 إلى جدول order
      - CHECK (overhead_cost_cents >= 0)
      - migration جديد 0005
      - في OrderForm: MoneyInput للـ overhead
      - في createOrder/updateOrder: totalCostCents = components sum + overheadCostCents
      - في OrderDetail: اعرض الـ overhead منفصلاً عن تكلفة المكونات
   
   و) عارض تقارير داخل الصفحة:
      - التقارير موجودة في 157a9e9 (downloadReport Server Action)
      - أضف ReportsClient يعرض التقرير داخل الصفحة + زر تحميل
      - التقارير يجب أن تُحسب server-side بـ SQL SUM()، لا client-side

3. لا تغير أي شيء آخر. لا ترحل إلى Vite. لا تنشئ API منفصل.
   لا تستخدم Bearer token. لا تخزّن أي سر في localStorage.
   كل تعديل مالي يجب أن يكون في transaction مع optimistic concurrency.

4. بعد كل ميزة، شغّل:
   - pnpm test (يجب أن تمر 27+ اختبار)
   - pnpm build (كل route ≤ 150KB)
   - تحقق أن migrations مطبّقة على قاعدة البيانات الحية

ابدأ بـ (1) ثم (أ) ثم (ب) — هذه هي الأولوية.